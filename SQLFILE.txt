SELECT * FROM SYSIBM.SYSPACKAGE                       
WHERE LASTUSED >= DATE(DAYS(CURRENT DATE) - 4)        
AND RELBOUND NOT IN ('P','Q','R')                     
WITH UR
;
SELECT * FROM FTS4DEV2.EMP
WHERE EMP_NAME Like 'DE%'
WITH UR
;
EXPLAIN STMTCACHE ALL     
;                         
