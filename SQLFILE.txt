SELECT CLOSERULE,
PARTITIONS, COUNT(*)   
FROM SYSIBM.SYSTABLESPACE    
WHERE PARTITIONS = 0         
GROUP BY CLOSERULE           
WITH UR;                                                                                        
