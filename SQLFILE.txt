SELECT * FROM SYSIBM.SYSPACKAGE                       
WHERE LASTUSED >= DATE(DAYS(CURRENT DATE) - 3)        
AND RELBOUND NOT IN ('P','Q','R')                     
WITH UR
;
                       
